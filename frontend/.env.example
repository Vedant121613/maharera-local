# Base URL of the backend API. Leave empty during local frontend-only
# development to automatically fall back to mock data (see src/api/client.ts).
#
# Example: VITE_API_BASE_URL=https://api.rera.example.com
VITE_API_BASE_URL=
